{
  "name": "sistema-deploy",
  "version": "1.0.0",
  "description": "Script de despliegue automatico",
  "scripts": {
    "deploy": "git pull origin main && echo '🚀 1. ACTUALIZANDO BACKEND...' && cd backend && npm install && npx pm2 restart ds-backend && cd .. && echo '🚀 2. RECONSTRUYENDO FRONTEND...' && cd frontend && npm install && npm run build && cd .. && echo '✅ DESPLIEGUE COMPLETADO CON EXITO!'"
  },
  "author": "Narabyte",
  "license": "ISC"
}